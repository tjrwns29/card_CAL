# 적용은 shift + alt + F

BasedOnStyle: Google  # Google 스타일을 기본으로 사용
ColumnLimit: 100      # 한 줄 최대 길이 (0은 자동 줄바꿈 비활성화)
UseTab: ForIndentation  # 들여쓰기에는 탭 사용, 나머지는 공백 사용
IndentWidth: 2        # 들여쓰기 크기 (탭이 2칸으로 설정됨)
TabWidth: 2           # 탭 너비를 2칸으로 설정
BreakBeforeBraces: Attach  # 중괄호 { 를 같은 줄에 배치 (Google 스타일 유지)
AllowShortFunctionsOnASingleLine: InlineOnly  # 짧은 함수는 한 줄로 유지
PointerAlignment: Left  # 포인터와 참조 연산자는 변수 이름이 아니라 타입과 붙이기 (int* ptr)
AlignConsecutiveAssignments: true  # 연속된 할당문 정렬
AlignConsecutiveDeclarations: true  # 연속된 변수 선언 정렬
SpaceBeforeParens: ControlStatements  # if, while, for 등 제어문에서만 괄호 앞에 공백 추가